# =============================================================================
# VARIAVEIS DE AMBIENTE - HELP DESK API
# =============================================================================
# Copie este arquivo para .env e preencha com os valores reais
# NUNCA commite o arquivo .env no Git!
# =============================================================================

# Profile do Spring
# dev = desenvolvimento (endpoint /api/test disponivel, logs detalhados)
# prod = producao (endpoint /api/test desabilitado, logs reduzidos)
SPRING_PROFILES_ACTIVE=dev

# Banco de Dados PostgreSQL
DB_URL=jdbc:postgresql://localhost:5432/helpdesk
DB_USERNAME=postgres
DB_PASSWORD=sua_senha_aqui

# JWT Secret (deve ter no mínimo 256 bits - 32 caracteres)
# Gere um secret forte: openssl rand -base64 32
JWT_SECRET=gere-um-secret-forte-aqui-minimo-32-caracteres

# CORS - Origens permitidas (separadas por vírgula)
# Desenvolvimento: múltiplas portas locais
# Produção: domínio específico (ex: https://seudominio.com)
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://localhost:5174,http://localhost:5175

# Configurações de Log (opcional - valores padrão em dev)
SHOW_SQL=true
FORMAT_SQL=true

# =============================================================================
# INSTRUÇÕES PARA GERAR JWT_SECRET FORTE:
# =============================================================================
# Linux/Mac:
#   openssl rand -base64 32
#
# Windows PowerShell:
#   [Convert]::ToBase64String((1..32 | ForEach-Object { Get-Random -Maximum 256 }))
#
# Online:
#   https://www.grc.com/passwords.htm
# =============================================================================
