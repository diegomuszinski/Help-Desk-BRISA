# =============================================================================
# CONFIGURACAO DE PRODUCAO
# =============================================================================
# Este arquivo e carregado automaticamente quando SPRING_PROFILES_ACTIVE=prod
# =============================================================================

# Logs reduzidos para producao
logging.level.org.springframework.security=WARN
logging.level.org.springframework.web.servlet.DispatcherServlet=WARN
logging.level.br.com.brisabr.helpdesk_api=INFO
logging.level.root=INFO

# NAO mostrar SQL em producao
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false

# CORS - Apenas dominio de producao
cors.allowed.origins=${CORS_ALLOWED_ORIGINS:https://seudominio.com}
