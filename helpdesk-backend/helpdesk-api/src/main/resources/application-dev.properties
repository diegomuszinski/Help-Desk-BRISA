# =============================================================================
# CONFIGURACAO DE DESENVOLVIMENTO
# =============================================================================
# Este arquivo e carregado automaticamente quando SPRING_PROFILES_ACTIVE=dev
# =============================================================================

# Logs detalhados para debugging
logging.level.org.springframework.security=DEBUG
logging.level.org.springframework.web.servlet.DispatcherServlet=DEBUG
logging.level.br.com.brisabr.helpdesk_api=DEBUG

# Mostrar SQL para desenvolvimento
spring.jpa.show-sql=${SHOW_SQL:true}
spring.jpa.properties.hibernate.format_sql=${FORMAT_SQL:true}

# CORS - Multiplas portas locais
cors.allowed.origins=${CORS_ALLOWED_ORIGINS:http://localhost:5173,http://localhost:5174,http://localhost:5175}
